apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
	compile 'org.apache.activemq:activemq-core:5.7.0', 'org.apache.activemq:activemq-core:5.7.0'
	//compile 'javax.jms:jms:1.1', 'javax.jms:jms:1.1'
}

task (run, dependsOn: 'classes', type: JavaExec) {
	main = 'Demo'
	classpath = sourceSets.main.runtimeClasspath
}
